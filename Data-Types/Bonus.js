//* ========== parseInt & parseFloat Section ==========

//? parseInt and parseFloat are both functions in JavaScript used for converting strings to numbers, but they have different use cases.

// var _value="42";
// console.log(parseInt(_value));

// we can also write like this 

const _secondValue= "4566.7";  // if int inside the double courts then it is always the string in java script
 const _store = parseInt(_secondValue);
  console.log(_store);


  const _valuefloat ="945308.78";
   const _my = parseFloat(_valuefloat);
      console.log(_my);